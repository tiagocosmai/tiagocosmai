/** Cor do título principal de cada secção (alinhado ao tema Matrix / verde). */
export function sectionHeadingClass(isDark: boolean): string {
  return isDark ? "text-[#00FF41]" : "text-[#14532d]";
}
